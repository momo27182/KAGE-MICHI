# KAGE-MICHI 開発ガイド

## プロジェクトの目的

都市の3Dデータ、太陽位置、気象情報を組み合わせ、時間帯ごとの日陰を考慮した徒歩ルートを案内する。単なる最短経路ではなく、暑熱曝露を減らす「最涼ルート」を提示し、熱中症対策につなげる。

## フォルダ構成

- `archive/hackathon_2025/`: 2025年ハッカソン時点の成果物。比較・再現用として保存し、原則直接編集しない。
- `src/`: これから作る製品コード。
- `tests/`: 自動テストと検証コード。
- `docs/`: 設計、調査、出典、ライセンス、評価結果。
- `data/`: 開発・検証用データ。出典と加工内容を記録する。

## 現在の基準実装

- GitHub `main`の最新ハッカソン版は `archive/hackathon_2025/github_main_snapshot/app.py` とする。
- 整理前のローカル基準アプリは `archive/hackathon_2025/app_realtime.py` とし、両者を比較できる状態で保存する。
- `app.py`、`app1.py`、`app2.py`、`app_blue*.py`、`app_L.py`、`app_s.py`、`app_search.py` はハッカソン中の派生版・試作版として扱い、内容を確認せず削除しない。
- `shadow_route.py`、`shadow_route_sub.py`、`shadow_test.py`、`map_test.py` は初期検証用スクリプトとして扱う。
- `shadow_navi.pptx` は発表資料、`動画/` はデモ記録である。
- `cache/` は取得データのキャッシュであり、製品の原本データとはみなさない。
- 上記のファイルとフォルダは、すべて `archive/hackathon_2025/` 内にある。

## 現在の技術構成

- Python / Streamlit
- OSMnx / OpenStreetMap: 歩行者道路、建物、コンビニ、給水地点
- GeoPandas / Shapely: 投影変換、影ポリゴン、空間判定
- Pysolar: 日時・座標から太陽高度と方位角を算出
- NetworkX: 日向区間に重みを加えた経路探索
- Folium / streamlit-folium: 地図表示と地点選択
- Geopy / Nominatim: 地名検索

## 開発時の基本方針

1. 作業開始時に `AGENT.md` と `log.md` を読む。
2. 実装・調査・判断・テスト結果は、その日の作業終了前に `log.md` へ追記する。
3. 新機能は `src/` に置き、UI、影計算、経路探索、データ取得を分離できる構造で実装する。新たな巨大な `app*.py` の複製は作らない。
4. 影や暑熱リスクに関する数値は安全情報である。根拠、単位、前提、データ時刻、推定であることを画面上で明示し、医学的な安全保証として表現しない。
5. PLATEAU、気象API、地図APIなどの利用規約・ライセンス・出典表示を確認し、配布物にも記載する。
6. 位置情報は必要最小限に扱い、保存する場合は目的、期間、削除方法を定める。
7. 変更後は最低限、構文確認、主要計算のテスト、代表地点・時刻での地図表示確認を行う。
8. 既存ファイルやキャッシュを削除・移動する前に、対象と復元方法を確認する。
9. GitHubでは`main`へ直接実装せず、目的ごとのブランチとPull Requestを使う。
10. `cache/`、動画、取得した巨大データ、APIキー、`.env`は通常のGitコミットへ含めない。

## 現状の重要な前提と課題

- 現コードは PLATEAU の3D都市モデルをまだ読み込んでいない。建物は OpenStreetMap 由来で、高さがない場合は10mとしている。
- 影は建物外形を影ベクトル方向へ平行移動し、その凸包を取る簡易モデルである。地形、樹木、庇、複雑な屋根、他の建物による遮蔽は扱わない。
- 道路の日陰判定は原則として各エッジの中央点のみであり、区間全体の日陰率ではない。
- 気温は手入力で、湿度、風、日射、WBGT、雲量、降水は経路コストに反映していない。
- 対象範囲は和歌山駅周辺の半径約1.7kmに固定され、起動時に外部サービスからデータを取得する。
- コードが複数の単一ファイル版に分岐しており、テスト、設定管理、依存バージョン固定、README、ライセンス表記が未整備である。

## 当面の優先順位

1. 現行試作の再現環境を固定し、基準動作と評価指標を定める。
2. コードをデータ取得・影計算・経路探索・UIに分割し、自動テストを追加する。
3. PLATEAU 建物データを小範囲で導入し、OSM簡易モデルとの差を検証する。
4. 気象データとWBGT等を導入し、暑熱コストの根拠を設計する。
5. 区間の日陰率を線分サンプリングまたは交差長で評価し、経路比較を改善する。
6. 実測または現地写真で影推定を検証し、誤差を公開できる形にする。
7. 対象地域拡大、キャッシュ、非同期処理、デプロイ、監視を設計する。

## 完了条件の例

- 新しい計算ロジックには、日中・夜間・高さ欠損・経路なし等のテストがある。
- 同じ入力データ・日時・設定から同じ結果を再現できる。
- 推定影と経路の算出根拠、データ出典、更新時刻、限界が利用者に見える。
- 代表ケースで速度と精度を測定し、結果を `log.md` に残している。
