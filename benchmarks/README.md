# 性能・回帰テスト用の基準ケース

`cases.json`は、同じ入力条件で速度と結果を比較するための正本です。

## 現在のケース

- 夏の午後の正常経路
- 同一路線の午前比較
- 夜間
- 出発地と目的地が同一
- 対象範囲外

座標はWGS84、日時はUTCオフセット付きISO 8601で記録します。実測値はケース定義へ直接書かず、`benchmark-results/`へ実行ごとのJSONとして保存する方針です。結果ファイルは環境差があるため、要約だけを`docs/performance-baseline.md`へ記録します。

